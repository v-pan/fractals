[package]
name = "fractals"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = { version = "0.27.0", features = ["fast-math"] }
iced = { version = "0.12.1", features = ["image", "advanced"] }

[lints.clippy]
needless_return = "allow"
